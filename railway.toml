[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn nudge_backend:app --bind 0.0.0.0:$PORT --workers 2"
healthcheckPath = "/book"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
